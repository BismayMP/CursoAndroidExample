<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/CadetBlue</item>
        <item name="colorPrimaryDark">@color/RoyalBlue</item>
        <item name="colorAccent">@color/pink_600</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="launcher" parent="Theme.AppCompat.Light.Dialog.Alert">

        <item name="android:actionBarTheme">@style/Theme.Design.NoActionBar</item>

    </style>

</resources>
